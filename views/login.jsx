var React = require('react');
var ReactDOM = require('react-dom');
var LoginComponent = React.createClass({displayName: 'Login',
					render: function(){
					    "use strict"
					    return
					    (React.createElement('form',{className: "user_login"}));
					}
					
});

module.exports = login;
